version: 2
project_name: cruxclaw

builds:
  - main: .
    binary: cruxclaw
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/cruxdigital-llc/openclaw-template/cli/cmd.Version={{.Version}}
      - -X github.com/cruxdigital-llc/openclaw-template/cli/cmd.Commit={{.Commit}}
      - -X github.com/cruxdigital-llc/openclaw-template/cli/cmd.Date={{.Date}}

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

release:
  github:
    owner: cruxdigital-llc
    name: openclaw-template
