.idea/
tfplan
*.tfstate
*.tfstate.backup
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform/backend.tf
terraform/terraform.tfvars

# CLI build artifacts
cli/conga
cli/dist/
bin/

# Terraform provider binary and secrets
cli/cmd/terraform-provider-conga/terraform-provider-conga
terraform.tfvars

# MCP config (user-specific)
.mcp.json
.mcp.json.*
!.mcp.json.example

# User-specific policy config
conga-policy.yaml

# Other local files
demo.*
